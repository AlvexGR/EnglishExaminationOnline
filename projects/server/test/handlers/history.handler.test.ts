import { assert } from "chai";
import { HistoryHandler } from '@server/src/handlers/history.handler';

describe("Test history handler", () => {
  const historyHandler = new HistoryHandler();

  it("get by user id", async () => {

  });

  it("get by id", async () => {

  });
});
